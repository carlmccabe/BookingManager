# BookingManager Application

To create this project I used Visual Studio **Standalone Typescript Angular Project** with an **Empty ASP.NET Web API Project**. I then added an **xUnit Test Project**

To build